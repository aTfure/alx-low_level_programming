#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @n: check the character
 *
 * Return: 0 Always(Success)
 */

int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}

	else
	{
		return (n);
	}
}
